/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['placehold.co', 'via.placeholder.com', 'picsum.photos', 'images.unsplash.com'],
    },
}

module.exports = nextConfig